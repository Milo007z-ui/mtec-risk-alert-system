# ระบบแจ้งเตือนจุดเสี่ยงอุบัติเหตุ — กรุงเทพฯ และปริมณฑล

เว็บแอปแผนที่ (Leaflet + OpenStreetMap) ติดตามตำแหน่ง GPS แบบเรียลไทม์
เมื่อเข้าใกล้จุดเสี่ยงอุบัติเหตุในระยะ **500 เมตร** จะแจ้งเตือนด้วย**เสียงพูดภาษาไทย**
พร้อมแบนเนอร์บนจอ ระบุสาเหตุความเสี่ยงและคำแนะนำการขับขี่เฉพาะจุด

ข้อมูลจุดเสี่ยงมาจากสถิติอุบัติเหตุปี 2568 ของ **MOT Data Catalog**
(ไฟล์ `data/accident2025_1.xlsx` รวม **4,627 เหตุการณ์** — 1 ชีตต่อจังหวัด:
กรุงเทพมหานคร นนทบุรี ปทุมธานี สมุทรปราการ นครปฐม สมุทรสาคร)
นำมาจัดกลุ่มด้วย **DBSCAN (รัศมี 400 ม. ขั้นต่ำ 3 เหตุการณ์)** ได้จุดเสี่ยง **171 จุด**
แล้วให้คะแนนด้วย**โมเดล v2**: 4 เกณฑ์ × 25% → **Percentile Rank** →
แบ่ง 3 ระดับที่ ≤40 / ≤60 / >60 (🔴 สูง 41 · 🟠 ปานกลาง 85 · 🟢 ต่ำ 45 จุด)

## วิธีใช้งาน

```bash
# 1) (ทำเมื่อถึงรอบ recalibration เท่านั้น — ดูหัวข้อโมเดล) สร้างไฟล์จุดเสี่ยงใหม่
pip install pandas openpyxl scikit-learn numpy
py scripts/build_risk_points.py            # อ่าน data/accident2025_1.xlsx -> data/risk_points_bkk_metro.geojson
py scripts/build_risk_points.py --selftest # ตรวจสูตรทั้งหมดก่อนใช้จริง

# 2) เปิด local server ที่โฟลเดอร์โปรเจกต์
py -m http.server 8123

# 3) เปิดเบราว์เซอร์
#    http://localhost:8123          <- ใช้งานจริง (GPS จริง)
#    http://localhost:8123/?mock=1  <- โหมดจำลอง: ขับรถผ่านจุดเสี่ยงอัตโนมัติ
```

กดปุ่ม **"เริ่มใช้งาน"** หนึ่งครั้งเพื่อปลดล็อกเสียงและเริ่มติดตามตำแหน่ง
(เบราว์เซอร์บังคับว่าเสียงต้องเริ่มจากการกดของผู้ใช้)

> **ทดสอบบนมือถือจริง:** Geolocation ใช้ได้เฉพาะ HTTPS หรือ localhost —
> deploy ขึ้น GitHub Pages (ได้ HTTPS ฟรี) หรือใช้ ngrok ชั่วคราว

## DBSCAN: หลักการจัดกลุ่มอุบัติเหตุให้เป็น "จุดเสี่ยง"

พิกัด GPS ของเหตุที่เกิด "จุดเดียวกัน" ในโลกจริงไม่ตรงกันเป๊ะ จึงต้องรวมเหตุการณ์
ที่เกิดใกล้กันให้เป็นจุดเสี่ยงเดียวก่อนให้คะแนน — ใช้ **DBSCAN**
(Density-Based Spatial Clustering of Applications with Noise) ซึ่งมองหา
"บริเวณที่จุดหนาแน่น" ด้วยพารามิเตอร์ 2 ตัว:

- **eps (ε)** — รัศมีเพื่อนบ้าน ที่นี่ใช้ **400 เมตร** (ระยะ haversine บนผิวโลก)
- **min_samples** — จำนวนเหตุการณ์ขั้นต่ำในรัศมี ที่นี่ใช้ **3**

อัลกอริทึมจำแนกทุกเหตุการณ์เป็น 3 ประเภท:

| ประเภท | เงื่อนไข | ผลต่อระบบ |
|---|---|---|
| **Core point** | มีเพื่อนบ้าน ≥ 3 เหตุการณ์ในรัศมี 400 ม. | เป็นแกนของจุดเสี่ยง และ "ลาม" ต่อไปยัง core อื่นที่อยู่ในรัศมีกันได้ (chain) |
| **Border point** | อยู่ในรัศมีของ core แต่เพื่อนบ้านตัวเองไม่ถึง 3 | ถูกนับรวมเข้าจุดเสี่ยงข้างเคียง |
| **Noise** | ไม่เข้าเงื่อนไขทั้งสอง | เหตุกระจัดกระจาย ไม่นับเป็นจุดเสี่ยง (รอบนี้ 676 จาก 4,460 เหตุการณ์) |

เหตุผลที่ DBSCAN เหมาะกับงาน accident hotspot กว่าวิธีอื่น (เช่น K-Means):

1. **ไม่ต้องกำหนดจำนวนกลุ่มล่วงหน้า** — เราไม่มีทางรู้ก่อนว่ากรุงเทพฯ มีจุดเสี่ยงกี่จุด
2. **จับกลุ่มรูปทรงอิสระได้** — อุบัติเหตุเรียงตัวเป็นแนวยาวตามถนน/ทางโค้ง ไม่ใช่วงกลม
3. **แยก noise ออกเองโดยธรรมชาติ** — เหตุการณ์เดี่ยวๆ ที่เกิดกระจัดกระจายไม่ถูกบังคับ
   ให้เข้ากลุ่มใดกลุ่มหนึ่ง (K-Means บังคับทุกจุดเข้ากลุ่ม ทำให้ centroid เพี้ยน)
4. **ขั้นต่ำ 3 เหตุการณ์** สอดคล้องนิยาม Black Spot มาตรฐานออสเตรเลีย
   (อุบัติเหตุบาดเจ็บ/เสียชีวิต ≥ 3 ครั้งในช่วงเวลาที่กำหนด)

ผลลัพธ์: 4,460 เหตุการณ์ที่มีพิกัด → **171 จุดเสี่ยง** (ครอบคลุม 3,784 เหตุการณ์)
พิกัดจุดเสี่ยง = ค่าเฉลี่ยพิกัดสมาชิก, สถิติทุกตัว (ผู้เสียชีวิต มูลค่าความเสียหาย ฯลฯ)
รวมจากสมาชิกทั้งหมดของกลุ่ม

## โครงสร้างโปรเจกต์

```
├── index.html                  # หน้าแผนที่ โหลด Leaflet จาก CDN
├── dashboard.html              # หน้าแดชบอร์ดสถิติ
├── css/  style.css · dashboard.css
├── js/
│   ├── distance.js             # Haversine + bounding box (ไม่แตะ DOM, test ได้ใน Node)
│   ├── riskpoints.js           # โหลด GeoJSON + วาด marker + popup รายละเอียดจุด
│   ├── riskrules.js            # กติกา Dynamic Alert: สาเหตุ + คำแนะนำรายจุด
│   ├── filters.js              # แผงตัวกรองมุมบนขวา (จังหวัด/ระดับ/ประเภทปัญหา)
│   ├── map.js                  # ตั้งค่าแผนที่ + marker ตำแหน่งผู้ใช้ + auto-pan
│   ├── gps.js                  # watchPosition + โหมดจำลอง (?mock=1)
│   ├── alert.js                # ตรวจรัศมี 500 ม. + cooldown กันเตือนซ้ำ
│   ├── tts.js                  # เสียงพูดไทย 3 ชั้น (Botnoi -> Google -> Web Speech)
│   ├── app.js                  # จุดเริ่มต้น ประกอบทุกโมดูล
│   └── dashboard.js            # สรุป KPI + กราฟ + ตาราง 10 อันดับ
├── data/
│   ├── accident2025_1.xlsx             # ข้อมูลต้นทาง 4,627 เหตุการณ์ (MOT, ปี 2568)
│   ├── risk_points_bkk_metro.geojson   # จุดเสี่ยง 171 จุด (ผลจาก pipeline)
│   ├── calibrations/v2568-r5.json      # snapshot ค่ารอบคำนวณ (เก็บทุกรอบไว้ตรวจย้อนหลัง)
│   └── mock_route.geojson              # เส้นทางถนนจริงสำหรับโหมดจำลอง
├── scripts/build_risk_points.py        # pipeline: Excel -> DBSCAN -> คะแนน v2 -> GeoJSON
├── docs/risk-score-criteria.md         # เกณฑ์โมเดล v2 + อ้างอิงทั้งหมด
├── api/server.py                       # REST API (FastAPI) ให้อุปกรณ์ดึงข้อมูลจุดเสี่ยง
├── device/pi_alert_client.py           # ไคลเอนต์ Raspberry Pi: GPS -> เรียก API -> พูดเตือน
└── tests/distance.test.js              # unit test: node tests/distance.test.js
```

## Risk Score โมเดล v2 (4 เกณฑ์ × 25% = 100 คะแนน)

แต่ละจุดได้คะแนนจาก 4 เกณฑ์อิสระต่อกัน ทุกเกณฑ์แปลงเป็น **Percentile Rank 0-100**
(อันดับเทียบกับจุดอื่นในรอบเดียวกัน — ทนต่อ outlier, OECD/JRC 2008) แล้วถ่วงน้ำหนักเท่ากัน

| เกณฑ์ | ค่าดิบต่อจุด | อ้างอิงหลัก |
|---|---|---|
| 1. ความถี่อุบัติเหตุ | จำนวนเหตุการณ์ในกลุ่ม | Black Spot ≥3 ครั้ง (ออสเตรเลีย) |
| 2. มูลค่าความเสียหาย | ตาย×6.7ล้าน + สาหัส×2ล้าน + เล็กน้อย×58,000 บาท | TDRI ปีงบประมาณ 2565 |
| 3. Single-Vehicle Ratio | % เหตุที่มีรถ ≤1 คัน | FHWA Roadway Departure |
| 4. ลักษณะถนน | % เหตุที่เกิดบริเวณทางแยก/ทางโค้ง/ทางร่วม/ต่างระดับ | TxDOT Conflict Points |

```
Percentile Rank = (อันดับเฉลี่ยของค่านั้น ÷ จำนวนจุดทั้งหมด) × 100      [tie ใช้ average rank]
Risk Score = 0.25×Freq + 0.25×Econ + 0.25×Single + 0.25×Geometry      (เต็ม 100)
```

**แบ่ง 3 ระดับด้วยจุดตัดคงที่:** 🟢 ต่ำ ≤ 40 < 🟠 ปานกลาง ≤ 60 < 🔴 สูง

> **ประวัติ:** r1–r2 Percentile + Jenks → r3 จุดตัดคงที่ 40/60 → r4 ทดลองตารางเกณฑ์
> → **r5 กลับมาใช้ Percentile Rank** (ตารางเกณฑ์ทำให้คะแนนหยาบเกินไป)
> เกณฑ์ที่ 4 ใช้ `conflict_pct` (% อ่านเข้าใจง่าย) แทน `gc_score` เดิม

**Fixed-Schedule Recalibration (หลักการสำคัญที่สุดของ v2):** ผลคะแนนทั้งหมด
คำนวณแล้ว "ล็อก" เป็นเวอร์ชัน
(`data/calibrations/v2568-r5.json`) — **ไม่คำนวณใหม่ทุกครั้งที่เปิดแอป**
ไม่งั้นคะแนนจุดเดิมจะลอยขึ้นลงเมื่อมีเหตุใหม่เข้าระบบแม้เหตุเดียว เปรียบเทียบข้ามเวลาไม่ได้
กำหนดรอบคำนวณใหม่ล่วงหน้า **ทุก 6 เดือน** (แนวทาง NCDOT/UNC HSRC 2011)
หน้าเว็บ (เรียลไทม์) แค่อ่านผลที่คำนวณไว้แล้ว — ท้ายหน้าแดชบอร์ดระบุเวอร์ชันรอบคำนวณกำกับไว้

รายละเอียดสูตรทุกเกณฑ์ + เหตุผลการเลือกวิธี + อ้างอิงครบ: **[docs/risk-score-criteria.md](docs/risk-score-criteria.md)**

## REST API สำหรับอุปกรณ์ (`api/server.py`)

เซิร์ฟเวอร์ FastAPI แจกจ่ายข้อมูลจุดเสี่ยงให้อุปกรณ์ภายนอก (Raspberry Pi, มือถือ ฯลฯ)
และเสิร์ฟหน้าเว็บเดิมไปพร้อมกัน — ใช้เซิร์ฟเวอร์เดียวแทน `py -m http.server` ได้เลย

```bash
pip install -r api/requirements.txt
python -m uvicorn api.server:app --host 0.0.0.0 --port 8000
# เว็บ:      http://localhost:8000
# API docs:  http://localhost:8000/docs   (Swagger UI อัตโนมัติ)
```

| Endpoint | ความหมาย |
|---|---|
| `GET /api/health` | สถานะเซิร์ฟเวอร์ + จำนวนจุดเสี่ยง |
| `GET /api/risk-points` | ทุกจุด กรองได้: `?level=high` `?province=นนทบุรี` `?min_score=55` |
| `GET /api/risk-points/nearby?lat=..&lng=..&radius=600` | จุดในรัศมี เรียงใกล้→ไกล พร้อม `distance_m` และ **`alert_message`** (ข้อความเตือนไทยสำเร็จรูป สร้างจากกติกา Dynamic Alert เดียวกับเว็บ) |
| `GET /api/risk-points/{id}` | รายละเอียดจุดเดียว + ปัจจัยเสี่ยง |

## ไคลเอนต์ Raspberry Pi (`device/pi_alert_client.py`)

สคริปต์สำหรับติดบนรถ (เช่น รถเมล์): อ่านพิกัด GPS ของรถ ถาม API ว่ามีจุดเสี่ยงใกล้ๆ ไหม
เมื่อเข้าใกล้กว่า 500 ม. จะพูดเตือนคนขับเป็นภาษาไทยผ่านลำโพง (espeak-ng)
ใช้กติกา cooldown ชุดเดียวกับหน้าเว็บ ใช้เฉพาะ Python standard library ไม่ต้อง pip install

```bash
# บน Raspberry Pi (ต่อ GPS USB + ลำโพง)
sudo apt install gpsd espeak-ng
python3 device/pi_alert_client.py --api http://<IP-เซิร์ฟเวอร์>:8000 --gpsd

# ทดสอบโดยไม่มี GPS: พิกัดคงที่ หรือจำลองเส้นทางจากไฟล์ (บรรทัดละ lat,lng)
python3 device/pi_alert_client.py --api http://localhost:8000 --test 13.665 100.534
python3 device/pi_alert_client.py --api http://localhost:8000 --route route.csv
```

## กติกาการแจ้งเตือน (ปรับได้ใน `js/alert.js`)

| ค่า | ความหมาย | ค่าเริ่มต้น |
|---|---|---|
| `ALERT_RADIUS_M` | รัศมีที่เริ่มเตือน | 500 ม. |
| `EXIT_RADIUS_M` | ต้องออกไกลกว่านี้ถึงจะ "เตือนซ้ำได้เมื่อกลับเข้ามา" (hysteresis) | 600 ม. |
| `REALERT_MS` | วนอยู่ในรัศมีเดิมนานเท่านี้ถึงเตือนซ้ำ | 5 นาที |

**Dynamic Alert** (`js/riskrules.js`): ข้อความเตือนประกอบจากกติกาตามเงื่อนไขจริงของจุด
เช่น ทางแยก/ทางโค้ง/จุดกลับรถ/ประวัติผู้เสียชีวิต/รถคันเดียวเสียหลักเป็นหลัก/ชนท้ายบ่อย
พร้อมคำแนะนำการขับขี่เฉพาะกรณี — ใช้ร่วมกันทั้งเสียงพูด แบนเนอร์ และ popup
ส่วน popup เพิ่ม**คำแนะนำเชิงวิศวกรรม**ตามรูปแบบปัญหา (Single-dominant → ไหล่ทาง/ราวกัน,
Multiple-dominant → สัญญาณไฟ/ช่องเลี้ยว)

## ตัวกรองจุดเสี่ยง (`js/filters.js`)

แผงมุมบนขวาของแผนที่ กรองได้ 3 แกน ใช้ร่วมกันได้:

| ตัวกรอง | ตัวเลือก |
|---|---|
| จังหวัด | ทุกจังหวัด หรือเลือก 1 ใน 6 จังหวัด (เติมอัตโนมัติจากข้อมูล) |
| ระดับความเสี่ยง | ติ๊กเลือกได้หลายระดับพร้อมกัน (สูง / ปานกลาง / ต่ำ) |
| ประเภทปัญหาหลัก | ทั้งหมด · Single-vehicle เด่น · Multiple-vehicle เด่น · ผสม |

ตัวกรองมีผล**ทั้งหมุดบนแผนที่และการแจ้งเตือน** — `alert.js` อ่านจาก `RiskPoints.visible()`
เพื่อให้ "เห็นอะไรบนแผนที่ = ได้ยินเตือนเรื่องนั้น" ไม่สับสน จำนวนจุดที่แสดงอยู่
บอกไว้ท้ายแผง (เช่น "แสดง 16 จาก 171 จุด") กดหัวแผงเพื่อย่อ/ขยาย และมีปุ่มล้างตัวกรอง

## การทดสอบ

```bash
py scripts/build_risk_points.py --selftest   # ตรวจสูตรโมเดล v2 (7 กลุ่มเคส)
node tests/distance.test.js                  # unit test ระยะทาง (9 เคส)
```

ทดสอบรวมทั้งระบบ: เปิด `http://localhost:8123/?mock=1` — ระบบจะวาดเส้นทางจำลอง
แล้วขับตามถนนจริง (บางปะอิน - แขวงรามอินทรา ~10.5 กม.) ซึ่งรอบ v2568-r5 มีจุดเสี่ยง
ตามแนวเส้นทางครบทั้ง 3 ระดับ ต้องได้ยินเสียงเตือน**หนึ่งครั้งต่อจุด**ตอนเข้าเขต 500 ม.
และไม่เตือนจุดเดิมซ้ำจนกว่าจะออกนอกเขตแล้วกลับเข้ามาใหม่
ปรับจังหวะช้า/เร็วได้ด้วย `?mock=1&mockpace=12` (วินาทีต่อกิโลเมตร — ค่ามากยิ่งช้า)
